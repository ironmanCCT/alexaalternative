type Note @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  description: String
  image: String
}

type phrase @model @auth(rules: [{ allow: public }]) {
  id: ID!
  word: String!
  description: String
  image: String
}
